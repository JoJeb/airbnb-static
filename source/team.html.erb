<div class="wrapper-grey padded">
  <div class="container">
    <ul class="list-inline text-center">
      <% ["romain", "jo", "spiderman"].each do |member| %>
        <li>
          <%= image_tag "#{member}.jpg", class: "avatar-large" %>
          <h3><%= member.capitalize %></h3>
        </li>
      <% end %>
    </ul>
  </div>
</div>
